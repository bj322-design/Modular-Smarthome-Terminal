> ### The static folder for now only has the CSS stylesheet,

> ### The templates folder will store the HTML files to be 'rendered'

  

### ensure that you have *(pip)* installed:  

-   flask 
    -   werkzeug *(dependent)*
    -   jinja *(dependent)*
    -   python-dotenv *(dependent)*